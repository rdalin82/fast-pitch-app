<div class="page-header">
  <h1><%= @presenter.name %>
  <%= @presenter.photo %>
  </h1>
  <div class="right">
</div>
<div>
<br>

<%= link_to "Add Presenter", "/presenters/new", class: "btn btn-success" %>
<%= link_to "Edit Presenter", "/presenters/#{@presenter.id}/edit", class: "btn btn-success" %>
<%= link_to "Delete Presenter", "/presenters/#{@presenter.id}", {method: :delete, class: "btn btn-danger" }%>

</div>
